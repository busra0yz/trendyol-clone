const express = require('express');
const router = express.Router();

// Şimdilik burası boş. Diğer endpoint'leri oluşturdukça buraya ekleyeceğiz.
// Örnek: router.use('/products', require('./product.routes'));

module.exports = router;